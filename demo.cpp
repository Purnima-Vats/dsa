#include<bits/stdc++.h>
using namespace std;
int main()
{
    int x;
    cin >> x;
    cout << "Hey " << x;
}

// Ctrl + Shift + B : to run the code
// Your cursor has to be on demo.cpp
// Your default terminal has to be cmd. It won't work if your default terminal is powershell.